name: tafseer_ibn_kaseer
description: তাফসীর ইবনে কাসীর - বাংলা কুরআন তাফসীর পাঠক
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  syncfusion_flutter_pdfviewer: ^28.1.33
  path_provider: ^2.1.3
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/surah_pages.json
